name: platform_adaptive_plus
description: A Flutter library providing adaptive platform utilities.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
